<!-- app/views/categories/index.html.erb -->

<h1>Categories</h1>
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category), remote: true %></li>
  <% end %>
</ul>

<div id="products-container"></div>
